* ensure that DC gets a grid square
* color individual squares by # pledges?

+ pre-compute cell for each zip code?
+ pre-compute selections of cells for each state?
+ render states in canvas and read pixels? (we could pre-compute this)
